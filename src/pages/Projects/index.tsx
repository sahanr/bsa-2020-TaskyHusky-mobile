import React from 'react';
import { Text } from 'react-native';
import DefaultPageWrapper from '../../containers/DefaultPageWrapper';

const Projects: React.FC = () => {
	return (
		<DefaultPageWrapper>
			<Text>Hello, I'm a test projects page</Text>
		</DefaultPageWrapper>
	);
};

export default Projects;
